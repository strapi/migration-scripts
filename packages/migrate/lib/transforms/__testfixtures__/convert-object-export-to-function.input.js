const file = require('file');

const someVar = { name: 'paul' };

const foo = () => {
  console.log(strapi);
};

function bar() {
  console.log(strapi);
}

module.exports = {
  foo,
  bar,
  plop() {
    console.log(strapi);
  },
  plunk() {
    console.log(strapi);
  }
};
